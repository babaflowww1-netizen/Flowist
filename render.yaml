services:
  - type: web
    name: real-panel
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python real_panel.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.4
      - key: TELEGRAM_BOT_TOKEN
        sync: false
